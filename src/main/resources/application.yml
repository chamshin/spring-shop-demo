spring:
datasource:
  url: ###
  username : ####
  password : ####
  driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace